<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🌊 OCEAN PUPPY CAFE 🐶 - 띠부씰 이벤트</title>
    <style>
        /* 테마별 색상 변수 정의 */
        :root {
            --bg-color: #f6f0e6; /* 따뜻한 모래사장 색 */
            --ocean-blue: #0077be; /* 시원한 바다 파랑 */
            --soda-blue: #a3d9ff; /* 크림소다 하늘색 */
            --text-brown: #5d4037; /* 진한 강아지 털색 브라운 */
            --accent-pink: #ffb7c5; /* 상큼한 포인트 핑크 */
        }

        body {
            font-family: 'HangangHand', 'Noto Sans KR', sans-serif; /* 귀여운느낌의 폰트 권장 */
            background-color: var(--bg-color);
            background-image: url('data:image/svg+xml;base64,'); /* 은은한 패턴 */
            color: var(--text-brown);
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* 헤더/배너 영역 */
        header {
            background: linear-gradient(to bottom, var(--ocean-blue), var(--soda-blue));
            color: white;
            width: 100%;
            padding: 40px 0;
            text-align: center;
            border-bottom: 5px solid white;
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
            position: relative;
        }

        header h1 {
            font-size: 3rem;
            margin: 0;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            letter-spacing: 2px;
        }

        header p { margin: 10px 0 0 0; font-size: 1.2rem; opacity: 0.9; }

        /* 강아지 아이콘 (예시) */
        .header-dog {
            position: absolute;
            bottom: -15px;
            right: 10%;
            font-size: 4rem;
        }

        /* 메인 컨텐츠 영역 */
        main {
            padding: 40px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            max-width: 1200px;
            width: 100%;
        }

        h2.section-title {
            font-size: 2rem;
            color: var(--ocean-blue);
            border-bottom: 3px solid var(--soda-blue);
            padding-bottom: 10px;
            margin-bottom: 30px;
        }

        /* 메뉴판 레이아웃 (Grid) */
        #menu-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            width: 100%;
            margin-bottom: 60px;
        }

        /* 메뉴 아이템 스타일 */
        .menu-item {
            background: rgba(255, 255, 255, 0.8);
            border: 2px solid var(--soda-blue);
            border-radius: 20px;
            padding: 25px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
            position: relative;
            overflow: hidden;
        }

        .menu-item:hover {
            transform: translateY(-8px) scale(1.02);
            background: #fff;
            box-shadow: 0 12px 24px rgba(0,0,0,0.15);
            border-color: var(--ocean-blue);
        }

        /* 바다 느낌 포인트 */
        .menu-item::before {
            content: '🌊';
            position: absolute;
            top: 10px;
            left: 10px;
            font-size: 1.2rem;
            opacity: 0.5;
        }

        .menu-item img {
            width: 100%;
            height: 180px;
            object-fit: cover;
            border-radius: 15px;
            margin-bottom: 15px;
            border: 3px solid white;
        }

        .menu-item h3 { margin: 10px 0 5px 0; font-size: 1.4rem; color: var(--text-brown); }
        .menu-item p { margin: 0; color: #8b7355; font-size: 0.95rem; line-height: 1.4; }
        
        .price-tag {
            background: var(--accent-pink);
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            display: inline-block;
            margin-top: 15px;
            font-weight: bold;
        }

        /* 뽑기 영역 및 애니메이션 */
        #draw-section {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 100%;
        }

        #result-area {
            text-align: center;
            padding: 30px;
            background: #fff;
            border-radius: 30px;
            border: 4px dashed var(--soda-blue);
            width: 90%;
            max-width: 450px;
            min-height: 300px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            position: relative;
        }

        /* 강아지 발바닥 포인트 */
        #result-area::after {
            content: '🐾';
            position: absolute;
            bottom: 15px;
            right: 15px;
            font-size: 2rem;
            opacity: 0.2;
        }

        #status { font-size: 1.3rem; margin-bottom: 20px; font-weight: bold; color: var(--ocean-blue); }

        /* 등장 애니메이션: 둥실둥실 떠오르며 나타남 */
        @keyframes ddibuAppear {
            0% { transform: translateY(50px) scale(0.5); opacity: 0; }
            70% { transform: translateY(-10px) scale(1.05); }
            100% { transform: translateY(0) scale(1); opacity: 1; }
        }

        /* 띠부씰 이미지 스타일 */
        #sticker-display {
            width: 200px;
            height: auto;
            display: none;
            border: 8px solid white;
            border-radius: 10px; /* 띠부씰 특유의 사각 느낌 */
            box-shadow: 0 6px 15px rgba(0,0,0,0.3);
            /* 스티커 테두리 효과 */
            filter: drop-shadow(0 0 2px rgba(0,0,0,0.5));
        }

        #sticker-display.show {
            display: block;
            animation: ddibuAppear 0.8s ease-out forwards;
        }

    </style>
</head>
<body>

    <header>
        <h1>🌊 OCEAN PUPPY CAFE 🐶</h1>
        <p>시원한 크림소다와 귀여운 강아지가 있는 곳</p>
        <div class="header-dog">⚓🐕</div>
    </header>

    <main>
        <h2 class="section-title">메뉴판</h2>

        <div id="menu-container">
            <div class="menu-item" onclick="orderAndDraw()">
                <img src="images/menu_soda.jpg" alt="블루 씨 소다">
                <h3>블루 씨 소다</h3>
                <p>바다를 닮은 푸른 크림소다에<br>바닐라 아이스크림 한 스쿱!</p>
                <div class="price-tag">6,000원</div>
            </div>

            <div class="menu-item" onclick="orderAndDraw()">
                <img src="images/menu_latte.jpg" alt="멍멍 크림 라떼">
                <h3>멍멍 크림 라떼</h3>
                <p>부드러운 크림 위에<br>귀여운 강아지 아트가 짠!</p>
                <div class="price-tag">5,500원</div>
            </div>

            <div class="menu-item" onclick="orderAndDraw()">
                <img src="images/menu_cookie.jpg" alt="조개 껍질 쿠키">
                <h3>조개 껍질 쿠키</h3>
                <p>바다에서 건져온 듯한<br>바삭하고 달콤한 버터 쿠키</p>
                <div class="price-tag">4,000원</div>
            </div>
            
        </div>

        <div id="draw-section">
            <h2 class="section-title">선물 뽑기</h2>
            <div id="result-area">
                <p id="status">메뉴를 클릭해 주문하시면<br>랜덤 띠부씰을 드립니다! 🎁</p>
                <img id="sticker-display" src="" alt="획득한 띠부씰">
            </div>
        </div>
    </main>

    <script>
        // [수정 필요] 준비하신 바다/강아지 테마 띠부씰 파일 경로들을 배열에 넣으세요.
        const stickers = [
            'stickers/puppy_surf.png',  // 서핑하는 강아지
            'stickers/puppy_soda.png',  // 소다 속 강아지
            'stickers/puppy_shell.png', // 조개 든 강아지
            'stickers/puppy_sailor.png' // 세일러 강아지
        ];

        function orderAndDraw() {
            // 1. 랜덤 선택
            const randomIndex = Math.floor(Math.random() * stickers.length);
            const selectedSticker = stickers[randomIndex];

            const imgTag = document.getElementById('sticker-display');
            const statusTag = document.getElementById('status');

            // 2. 초기화 (이전 애니메이션 제거)
            imgTag.classList.remove('show');
            void imgTag.offsetWidth; // 강재 리플로우

            // 3. 스티커 표시 및 애니메이션 적용
            imgTag.src = selectedSticker;
            imgTag.classList.add('show');
            
            // 4. 상태 메시지 업데이트
            statusTag.innerHTML = "🎊 <b>바다 강아지 띠부씰</b>을<br>획득했습니다! 🎊";
        }
    </script>

</body>
</html>